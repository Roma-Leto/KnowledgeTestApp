$Какой тип данных используется для хранения целых чисел в Python?,-float,-int,+int,-str
$Какой оператор используется для возведения числа в степень?,-*,/,+**
$Каким образом в Python можно обработать исключение деления на ноль?,-try-except,-if-else,+try-except-else,-if-else-except
$Какой метод используется для добавления элемента в список?,-add,+append,-insert,-push
$Что выводит следующий код: `print(5 > 3 and 10 < 20)`?,-False,+True,-None,-Error
$Какая структура данных в Python хранит уникальные элементы?,-list,+set,-tuple,-dict
$Как правильно записать цикл, который будет перебирать элементы списка `my_list`?,-for item in my_list,+for item in my_list:,-while item in my_list,-while item
$Что происходит при попытке выполнить деление на ноль в Python?,-Возникает ошибка ValueError,+ZeroDivisionError,-Итогом деления будет None,-Программа продолжит выполнение
$Какой метод позволяет удалить элемент из словаря по ключу?,-popitem,+pop,-remove,-delete
$Как записать строку, которая включает кавычки внутри самой строки?,-'Hello, "Python"' ,- "Hello, 'Python'" ,+ 'Hello, "Python"'
$Какую команду нужно использовать для вывода текста в консоль?,-output("Hello") ,+print("Hello") ,-echo "Hello",-console.write("Hello")
$Как создать пустой словарь?,-dict() ,+{} ,-[],-set()
$Какой метод строки используется для преобразования всех символов в строке в верхний регистр?,-to_upper,+upper,-uppercase,-toupper
$Какой тип данных в Python неизменяем?,-list,+tuple,-set,-dict
$Что будет выведено в результате выполнения `print(2 == 2)`?,-False,+True,-None,-Error
$Какой оператор используется для проверки равенства в Python?,-= ,+==,-!=,-<>
$Как объявить переменную, которая будет хранить строку "Hello World"?,-var = "Hello World", +my_string = "Hello World",-str = "Hello World",-text = "Hello World"
$Как в Python создать цикл, который будет работать, пока условие истинно?,-for i in range(10): ,+while True:, -while i > 0 , -repeat until condition
$Что выведет код: `print("a" in "apple")`? ,+True, -False, -None, -Error
$Что делает конструкция `try-except`? ,+Обрабатывает исключения, -Проверяет ошибки в коде, -Завершает выполнение программы при ошибке, -Перехватывает все ошибки и игнорирует их
$Как получить длину строки в Python? ,+len("Hello"), -string_length("Hello"), -string.size("Hello"), -length("Hello")
$Как добавить новый элемент в конец списка `lst = [1, 2, 3]`? ,-lst.add(4), -lst.insert(4), +lst.append(4), -lst.append([4])
$Что делает оператор `is` в Python? ,-Сравнивает значения объектов, +Проверяет идентичность объектов, -Проверяет тип объектов, -Проверяет, равны ли элементы
$Как объявить и присвоить значение переменной в Python? ,+x = 5, -int x = 5, -var: 5 , -x := 5
$Что выведет `print(3 * "Python")`? ,-PythonPythonPython, +PythonPythonPython , -Error, -None
$Как создать кортеж с одним элементом? ,-tuple(1), + (1,), -[1], -tuple([1])
$Какая команда используется для импорта модуля в Python? ,-include math , +import math , -use math , -from math import *
$Какой тип данных представляет собой изменяемую последовательность? ,-tuple, +list, -set, -frozenset
$Как завершить выполнение программы с кодом ошибки? ,-exit(1), +sys.exit(1), -break(1), -return(1)
$Как обработать несколько исключений в одном блоке `except`? ,+except (ValueError, TypeError), -except(ValueError or TypeError), -except (ValueError & TypeError), -except (ValueError, TypeError) as e
$Что выведет код: `print([1, 2] + [3, 4])`? ,-[1, 2, 5, 6], +[1, 2, 3, 4], -Error, -None
$Как записать строку с несколькими строками в Python? ,+'''Многострочная строка''', -"\nМногострочная строка\n", -["Многострочная строка"], -""\nМногострочная строка
$Как проверить, является ли объект строкой в Python? ,-is_string() ,+ isinstance(x, str), -str(x), -x.is_string()
$Как создать пустой список? ,-list() ,+[], -dict(), -set()
$Что такое объект в Python? ,+Экземпляр класса, -Переменная, -Функция, -Тип данных
$Как провести тестирование кода с использованием библиотеки `unittest`? ,+Нужно создать класс, наследующий `unittest.TestCase` и написать методы с префиксом `test_`, -Нужно использовать команду `unittest.run()`, -Тестирование невозможно с помощью `unittest`, -Методы не должны начинаться с `test_`
$Что произойдет при делении целых чисел 10 на 3 в Python? ,+Результат будет 3.333333..., -Будет округлено до целого числа 3, -Возникнет ошибка, -Результат будет строкой
$Каким образом можно получить элементы из списка по индексу? ,+list[index], -list{index}, -list: index, -list index
$Что делает метод `split()` у строки? ,+Разбивает строку на подстроки по разделителю, -Применяет разделитель к строкам, -Объединяет строки, -Удаляет пробелы
$Как создать исключение в Python? ,+raise MyError("message"), -throw MyError("message"), -raise error("message"), -error("message")
$Что такое рекурсия? ,+Функция вызывает сама себя, -Функция работает в цикле, -Функция возвращает новый объект, -Функция вызывает другие функции
$Как удалить элемент по индексу в списке? ,-remove(2), +pop(2), -delete(2), -del(2)
$Какая структура данных в Python используется для хранения пар "ключ-значение"? ,+dict, -set, -list, -tuple
$Каким образом можно сравнить два объекта в Python? ,+через `==`, -через `is`, -через `cmp`, -через `equals`
$Как правильно написать цикл, который будет повторяться 5 раз? ,-for i in range(5):, +for i in range(5): , -for i = 1 to 5 : , -while i < 5 :
$Что возвращает метод `.find()` у строки? ,+Индекс первого вхождения подстроки, -True или False, -Список подстрок, -Индекс последнего вхождения подстроки
$Как удалить элемент из множества? ,-remove(2), +my_set.remove(2), -pop(2), -del(2)
$Как вывести строку в консоль без перевода на новую строку? ,-print("Hello"), +print("Hello", end=""), -print("Hello", newline=""), -echo("Hello")
$Каким образом можно проверить, что объект является целым числом? ,+isinstance(x, int), -type(x) == "int", -x.isdigit(), -x.is_integer()
$Каким образом можно узнать тип объекта в Python? ,+type(x), -object_type(x), -typeOf(x), -gettype(x)
$Что вернется в результате выполнения `3 == 3`? ,+True, -False, -None, -Error
$Каким образом можно выйти из цикла в Python? ,+с помощью `break`, -с помощью `exit()`, -с помощью `continue`, -с помощью `return`
$Какой оператор используется для вычисления остатка от деления? ,+%, -//, -/, -*
$Какой тип данных в Python является неизменяемым и упорядоченным? ,+tuple, -list, -dict, -set
$Как в Python преобразовать строку в число с плавающей точкой? ,+float("3.14"), -int("3.14"), -str(3.14), -round("3.14")
$Что будет выведено в результате `print("Hello" * 3)`? ,+HelloHelloHello, -Hello, -HelloHello, -Error
$Как записать блок кода в Python, который выполнится, если переменная `x` больше 10? ,-if x > 10: ,+if x > 10:, -if x > 10 , -if(x > 10):
$Какой оператор используется для проверки неравенства в Python? ,+!=, -<>, -!==, -not=
$Как правильно создать пустое множество в Python? ,+set(), -{}, -list(), -[]
$Как получить элемент по индексу в строке в Python? ,+string[index], -string[index+1], -index.string(), -string.get(index)
$Какую функцию следует использовать для вывода целого числа в консоль? ,+print(int), -print("int"), -output(int), -console(int)
$Как можно объединить два списка в Python? ,+list1 + list2, -list1.append(list2), -list1.extend(list2), -list1.insert(list2)
$Что возвращает функция `abs()`? ,+Модуль числа, -Квадрат числа, -Округление числа, -Целую часть числа
$Что будет выведено при выполнении `print([1, 2, 3] == [1, 2, 3])`? ,+True, -False, -None, -Error
$Какой тип данных используется для хранения чисел с плавающей точкой? ,+float, -int, -bool, -str
$Какой оператор в Python используется для объединения строк? ,++, -&, -concat, -append
$Как можно получить остаток от деления двух чисел? ,+5 % 2, -5 // 2, -5 / 2, -5 ** 2
$Как можно определить длину списка в Python? ,+len(my_list), -list.size(), -len(my_list()), -list.length()
$Какой метод используется для вставки элемента в список на определенную позицию? ,+insert(), -add(), -append(), -place()
$Какой оператор используется для выполнения целочисленного деления в Python? ,+//, -/, -%, -**
$Как создать пустой кортеж в Python? ,+(), -tuple(), -[] , -set()
$Какое исключение возникает при попытке обращения к несуществующему индексу в списке? ,+IndexError, -ValueError, -KeyError, -TypeError
$Что выводит код `print(5 == 5.0)`? ,+True, -False, -None, -Error
$Как в Python определить, является ли объект числом? ,+isinstance(x, (int, float)), -x.is_number(), -type(x) == "int", -x.isnumeric()
$Как вызвать функцию внутри функции? ,+func(), -call(func), -exec(func), -apply(func)
$Как называется способ передачи аргументов функции, когда аргументы передаются по имени? ,+Ключевые аргументы, -Позиционные аргументы, -Массивные аргументы, -Нумерованные аргументы
$Что делает функция `range()` в Python? ,+Создает последовательность чисел, -Возвращает строку, -Создает список, -Создает кортеж
$Как объявить глобальную переменную в Python? ,+global x, -extern x, -global var, -var = global
$Что будет выведено в результате `print("apple".replace("p", "x"))`? ,+axxle, -axple, -apxle, -Error
$Как можно объединить несколько строк в одну в Python? ,+''.join(['str1', 'str2']), -str1 + str2, -concatenate('str1', 'str2'), -append('str1', 'str2')
$Что означает использование ключевого слова `self` в методах классов? ,+Ссылка на текущий объект класса, -Переменная, хранящая данные, -Ссылка на другой объект, -Ссылка на метод класса
$Как объявить функцию, которая не принимает аргументов? ,+def my_function():, -def my_function(), -def my_function(,):, -def my_function()
$Как выполнить проверку, является ли объект пустым списком? ,+if not my_list:, -if my_list == 0:, -if len(my_list) > 0:, -if len(my_list) == 0:
$Как обработать исключение в блоке `try-except` и вывести сообщение ошибки? ,+except Exception as e: print(e), -catch Error as e: print(e), -throw Error as e: print(e), -finally Error as e:
$Как создать объект класса в Python? ,+my_obj = MyClass(), -my_obj = MyClass, -obj = new MyClass(), -obj = create(MyClass)
$Какая команда используется для удаления элемента из списка по индексу? ,+del list[index], -remove(list[index]), -pop(index), -del(list.index)
$Какое исключение возникает при попытке обратиться к несуществующему ключу в словаре? ,+KeyError, -ValueError, -IndexError, -TypeError
$Какой метод используется для получения всех значений из словаря? ,+dict.values(), -dict.get_values(), -dict.all_values(), -dict.list_values()
$Как вставить новый элемент в начало списка? ,+my_list.insert(0, item), -my_list.append(item), -my_list.add(0, item), -my_list.insert(item, 0)
$Как правильно обрабатывать исключения, если их несколько? ,+except (Error1, Error2), -except Error1 & Error2, -except Error1, Error2, -except Error1|Error2
$Что вернет вызов `len("Python")`? ,+6, -7, -5, -None
$Как проверить, что строка является цифрой в Python? ,+my_string.isdigit(), -my_string.is_number(), -is_number(my_string), -isdigit(my_string)
$Как вывести элементы списка на экран через пробел? ,+print(*my_list), -print(my_list), -print(my_list.join(" ")), -join(my_list, " ")
$Что выведет следующий код: `print(bool(0))`? ,+False, -True, -None, -Error
$Как правильно записать строку в файл в режиме записи? ,+with open("file.txt", "w") as f: f.write("Hello"), -open("file.txt", "w"): f.write("Hello"), -write("Hello", "file.txt"), -with open("file.txt"): f.write("Hello")
$Как получить все ключи словаря? ,+dict.keys(), -dict.get_keys(), -dict.all_keys(), -dict.key_set()
$Что выведет код `print("abc" > "abd")`? ,+False, -True, -None, -Error
$Как вызвать исключение в Python? ,+raise Exception("message"), -throw Exception("message"), -error("message"), -catch Exception("message")
$Как можно создать переменную с неизменяемым значением в Python? ,+Присвоить значению кортеж, -Присвоить значению список, -Присвоить значению множество, -Присвоить значению строку
$Какой метод используется для удаления элемента из множества? ,+remove(), -discard(), -pop(), -delete()
$Как сделать элемент списка неизменяемым в Python? ,+Превратить его в кортеж, -Использовать функцию `immutable()`, -Присвоить значение через `const`, -Не существует такой возможности
$Что делает метод `.strip()` у строки? ,+Удаляет пробелы в начале и в конце строки, -Удаляет все пробелы в строке, -Удаляет пробелы только в конце строки, -Удаляет пробелы только в начале строки
$Как обработать все исключения с одним блоком `except`? ,+except Exception as e:, -except *, -catch Exception as e:, -except all
$Как добавить элементы в конец множества? ,+set.add(item), -set.append(item), -set.push(item), -set.insert(item)
$Каким образом можно вызвать функцию с переменным числом аргументов? ,+def my_func(*args), -def my_func(args), -def my_func(...args), -def my_func(arg1, arg2)
$Как получить доступ к элементу словаря по ключу? ,+dict[key], -dict.key, -dict.get(key), -dict.item(key)
$Как реализовать цикл по элементам кортежа? ,+for item in my_tuple:, -for item in my_tuple(), -for item in (my_tuple):, -for item in tuple(my_tuple):
$Как можно преобразовать список в строку с разделителями? ,+",".join(my_list), -my_list.join(","), -join(my_list, ","), -str(my_list)
$Что такое декоратор в Python? ,+Функция, изменяющая поведение другой функции, -Переменная, -Тип данных, -Модуль
$Какой метод используется для получения первого вхождения подстроки в строке? ,+find(), -index(), -search(), -locate()
$Как создать метод класса? ,+def method(self):, -def method():, -method(self):, -create_method(self):
$Что означает метод `.pop()` у списка? ,+Удаляет и возвращает элемент по индексу, -Добавляет элемент в конец списка, -Удаляет последний элемент, -Удаляет элемент по значению
$Как изменить значение глобальной переменной внутри функции? ,+global x, -extern x, -var x, -declare x
$Как создать список из чисел от 0 до 9 с помощью `range()`? ,+list(range(10)), -range(10).list(), -list[range(10)], -range(10).to_list()
$Что будет выведено при выполнении `print(bool([]))`? ,+False, -True, -None, -Error
$Что происходит при вызове `open("file.txt", "r")`? ,+Открывается файл для чтения, -Открывается файл для записи, -Открывается файл для добавления, -Ошибка открытия файла
$Какой оператор используется для логического "ИЛИ"? ,+or, -and, -xor, -nor
$Как узнать количество элементов в множестве? ,+len(my_set), -set.size(), -len(set), -count(my_set)
$Как добавить новый элемент в словарь? ,+dict[key] = value, -dict.add(key, value), -dict.insert(key, value), -dict.append(key, value)
$Как импортировать только одну функцию из модуля? ,+from module import function, -import module.function, -from function import module, -import function
$Какое значение будет возвращено выражением `True and False`? ,+False, -True, -None, -Error
$Как проверить, является ли строка пустой в Python? ,+if not my_string:, -if len(my_string) == 0:, -if my_string == "":, -if my_string.empty():
$Как проверить, является ли объект числом с плавающей точкой? ,+isinstance(x, float), -type(x) == "float", -x.isfloat(), -x.is_number()
$Как изменить регистр первого символа строки в верхний? ,+my_string.capitalize(), -my_string.upper(), -my_string.title(), -my_string.first_upper()
$Как вызвать метод для работы с файлом, если файл открыт в режиме чтения? ,+file.read(), -file.write(), -file.append(), -file.open()
$Как вывести все элементы множества на экран в Python? ,+print(*my_set), -print(my_set), -print(my_set.elements()), -print(list(my_set))
$Что будет выведено при выполнении `print(len([1, 2, 3]))`? ,+3, -2, -None, -Error
$Какой оператор используется для объединения списков? ,+`+`, -`,`, -`&`, -`*`
$Что возвращает метод `dict.get()`? ,+Значение по ключу, -Ключ по значению, -Список всех значений, -Все ключи словаря
$Как получить индекс элемента в списке? ,+my_list.index(item), -my_list.find(item), -index(item), -list.index(item)
$Что выведет код `print([1, 2] * 3)`? ,+[1, 2, 1, 2, 1, 2], -[1, 1, 1], -[1, 2, 3], -None
$Как определить длину множества? ,+len(my_set), -set.size(), -size(my_set), -len(set)
$Как создать множество из списка? ,+set(my_list), -list(my_set), -set(list(my_set)), -my_set.create()
$Как получить значение элемента в словаре по ключу с использованием метода `get()`? ,+my_dict.get(key), -my_dict[key], -my_dict.fetch(key), -my_dict.access(key)
$Как объединить два множества? ,+set1 | set2, -set1 + set2, -set1.add(set2), -set1.append(set2)
$Что делает метод `.sort()` у списка? ,+Сортирует элементы списка, -Удаляет элементы списка, -Меняет порядок элементов, -Переводит все элементы в строковый формат
$Что произойдет, если вызвать `pop()` на пустом списке? ,+Возникнет ошибка IndexError, -Возвращается `None`, -Будет возвращено последнее значение, -Программа продолжит выполнение
$Как добавить элементы из другого списка в существующий список? ,+my_list.extend(other_list), -my_list.add(other_list), -my_list.append(other_list), -my_list.insert(other_list)
$Как получить доступ к первому элементу кортежа? ,+my_tuple[0], -my_tuple[1], -my_tuple.get(0), -my_tuple.first()
$Как найти максимальный элемент в списке? ,+max(my_list), -min(my_list), -list.max(), -list.find_max()
$Что выведет код `print("abc" < "abd")`? ,+True, -False, -None, -Error
$Как правильно записать условие, чтобы проверить, является ли переменная числом? ,+if isinstance(x, (int, float)):, -if isinstance(x, int):, -if x.isdigit():, -if x.type() == "number":
$Как преобразовать строку в верхний регистр? ,+my_string.upper(), -my_string.capitalize(), -my_string.lower(), -my_string.title()
$Как обработать исключение при открытии файла? ,+try: open("file.txt", "r"), -except IOError as e: print(e), -with open() as f: except IOError, -open("file.txt", "r") as f: except IOError
$Как объединить несколько строк в одну? ,+''.join([str1, str2, str3]), -str1.concat(str2, str3), -str1 + str2 + str3, -combine(str1, str2, str3)
$Как создать кортеж с несколькими элементами? ,+tuple([1, 2, 3]), -tuple(1, 2, 3), -tuple{1, 2, 3}, -tuple{1, [2, 3]}
$Как вернуть несколько значений из функции? ,+return val1, val2, -return [val1, val2], -return (val1, val2), -return {val1, val2}
$Как создать словарь с ключами и значениями? ,+my_dict = {"key1": "value1", "key2": "value2"}, -my_dict = dict(key1="value1", key2="value2"), -my_dict = {key1: value1, key2: value2}, -dict = {"key1", "value1", "key2", "value2"}
$Как добавить элемент в начало списка? ,+my_list.insert(0, item), -my_list.append(item), -my_list.push(item), -my_list.add(item)
$Как удалить элемент из словаря? ,+del my_dict[key], -my_dict.remove(key), -my_dict.pop(key), -my_dict.delete(key)
$Как получить все ключи из словаря? ,+my_dict.keys(), -my_dict.get_keys(), -my_dict.list_keys(), -my_dict.all_keys()
$Как удалить все элементы из множества? ,+my_set.clear(), -my_set.remove(), -my_set.delete(), -my_set.pop()
$Как обработать исключения с различными типами ошибок? ,+except (Error1, Error2):, -except Error1 or Error2:, -except (Error1, Error2) as e:, -except Error1, Error2:
$Как вернуть остаток от деления в Python? ,+5 % 2, -5 // 2, -5 / 2, -5 ** 2
$Что делает метод `.join()` у строки? ,+Объединяет элементы списка в строку, -Разделяет строку на элементы, -Переводит строку в список, -Заменяет все символы на указанный
$Как извлечь элемент из списка с помощью индекса? ,+my_list[index], -my_list.get(index), -index(my_list), -my_list.extract(index)
$Как проверить, является ли объект числом с плавающей точкой? ,+isinstance(x, float), -type(x) == "float", -x.isfloat(), -x.is_number()
$Как удалить последний элемент из списка? ,+my_list.pop(), -my_list.remove(), -my_list.delete(), -my_list.erase()
$Как создать список, содержащий все элементы от 0 до 9? ,+list(range(10)), -range(10).list(), -list[range(10)], -range(10).to_list()
$Как проверить, что строка является числом? ,+my_string.isdigit(), -my_string.is_number(), -is_number(my_string), -isdigit(my_string)
$Что делает метод `.split()` у строки? ,+Разбивает строку на части по разделителю, -Объединяет строки, -Разделяет строку по индексу, -Убирает пробелы
$Как получить наибольшее значение из списка? ,+max(my_list), -min(my_list), -my_list.max(), -list.max()
$Как создать новый список с результатами применения функции ко всем элементам исходного списка? ,+list(map(func, my_list)), -map(list(func), my_list), -my_list.apply(func), -list.apply(func)
$Как добавить элемент в список по индексу? ,+my_list.insert(index, item), -my_list.append(item), -my_list.add(index, item), -my_list.push(index, item)
$Как создать пустое множество в Python? ,+set(), -{}, -[], -()
$Что делает метод `.strip()` у строки? ,+Удаляет пробелы с начала и конца строки, -Удаляет все пробелы в строке, -Удаляет пробелы только с начала строки, -Удаляет пробелы только с конца строки
$Как вызвать исключение с пользовательским сообщением? ,+raise Exception("message"), -throw Exception("message"), -error("message"), -catch Exception("message")
$Как получить количество вхождений элемента в список? ,+my_list.count(item), -my_list.frequency(item), -my_list.find(item), -my_list.find_count(item)
$Что будет выведено при выполнении `print("123".isnumeric())`? ,+True, -False, -None, -Error
$Как объединить два множества в Python? ,+set1 | set2, -set1.add(set2), -set1.append(set2), -set1.extend(set2)
$Как получить максимальное значение из словаря по значению? ,+max(my_dict, key=my_dict.get), -max(my_dict), -my_dict.max(), -max(my_dict.values())
$Как получить строковое представление объекта в Python? ,+str(obj), -repr(obj), -object(obj), -convert(obj)
$Как найти минимальный элемент в списке? ,+min(my_list), -max(my_list), -list.min(), -list.find_min()
$Как сравнить два списка на равенство? ,+list1 == list2, -list1.equals(list2), -list1.compare(list2), -list1.is_equal(list2)
$Как извлечь последние два элемента из списка? ,+my_list[-2:], -my_list[2:], -my_list[-1:], -my_list[0:2]
$Как объединить несколько строк в одну с разделителем? ,+','.join([str1, str2, str3]), -str1.concat(str2, str3), -str1 + str2 + str3, -combine(str1, str2, str3)
$Как проверить, является ли список пустым? ,+if not my_list:, -if my_list == [], -if len(my_list) == 0:, -if my_list.empty():
$Как создать словарь с помощью функции `dict()`? ,+my_dict = dict(key1="value1", key2="value2"), -my_dict = dict(["key1", "value1"], ["key2", "value2"]), -my_dict = dict({"key1": "value1", "key2": "value2"}), -my_dict = dict(key1 = value1, key2 = value2)
$Как присвоить значение переменной, если она еще не была определена? ,+my_var = my_var if my_var is not None else default_value, -my_var = my_var or default_value, -my_var = None if my_var is None else default_value, -default_value if my_var is None
